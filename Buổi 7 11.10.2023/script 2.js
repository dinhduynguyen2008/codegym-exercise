let length = parseInt(prompt("Please input length"));
let width = parseInt(prompt("Please input width"));

let perimeter = (length + width) * 2;
let area = length * width;

alert("perimeter:");
alert (perimeter);

alert("area:");
alert (area);